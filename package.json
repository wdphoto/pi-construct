{
  "name": "the-construct",
  "version": "0.0.0",
  "private": true,
  "description": "A global Pi extension for loading project-local Pi resources.",
  "type": "module",
  "pi": {
    "extensions": ["src/index.ts"]
  },
  "scripts": {
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typescript": "^5.0.0"
  }
}
